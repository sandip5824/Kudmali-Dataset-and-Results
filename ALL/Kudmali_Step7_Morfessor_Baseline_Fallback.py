# Reproducible Step 7 fallback
# NOTE: This is an MDL-style Morfessor-Baseline-compatible fallback, not the official
# Morfessor 2.0.6 package. Replace with the official package run when package installation
# is available, while keeping the frozen dataset/split unchanged.
